# Test-only dependencies. Not needed at runtime — install alongside
# requirements.txt for local development and CI:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=7.0.0
httpx>=0.28.1
